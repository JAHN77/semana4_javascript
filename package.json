{
  "name": "mini-app-crud",
  "version": "1.0.0",
  "description": "Mini aplicación web con persistencia y consumo de APIs",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
